# Real Turnstile site key for production. Public by design -- committed on purpose, unlike
# the secret key (TURNSTILE_SECRET), which is a Pages secret and never lives in the repo.
VITE_TURNSTILE_SITEKEY=0x4AAAAAAExqMzuO-KH3xm-s
