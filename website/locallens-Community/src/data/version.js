// Single source of truth for the shipped version number. Imported by the header, the
// status bar boot line, and the `version` command — never hard-code '3.0.2' elsewhere.
export const APP_VERSION = '3.0.2'
