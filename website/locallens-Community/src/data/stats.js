// Stub seam for Task 7. No source is wired up yet, so every value is honestly unknown —
// StatusBar and the `stats` command render '—' for each null rather than a fabricated number.
export function getStats() {
  return { stars: null, views: null, repoViews: null, openPosts: null }
}
