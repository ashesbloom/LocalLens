// Placeholder — Task 3 replaces this with the real homepage (hero + section list).
export default function Home() {
  return (
    <>
      <h1 className="h1">Home</h1>
      <p className="lede">
        The full homepage lands in a later task. Use the nav above, or type a command like{' '}
        <code>ls</code> or <code>help</code>, to look around now.
      </p>
    </>
  )
}
