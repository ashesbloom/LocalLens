fastapi
uvicorn
numpy>=1.26.4,<2
pydantic
Pillow
face_recognition
face_recognition_models
reverse_geocoder
pillow-heif
rawpy; sys_platform == 'win32'
Wand; sys_platform != 'win32'
python-multipart
PyInstaller==6.19.0
setuptools<81
send2trash
# PDF export (/api/export-report -> report_pdf.py). Undeclared until now, so it was
# absent from every packaged build while working fine in dev venvs — the same shape
# as the imagehash bug. main.py's ImportError -> 501 handler stays as the fallback.
reportlab


# ============================================================================
# Platform-Specific Dependencies:
# ============================================================================
# - rawpy: Used on Windows for RAW image processing (automatic)
# - Wand: Used on macOS/Linux for RAW image processing (requires ImageMagick)
#
# macOS Setup (REQUIRED for Apple Silicon M1/M2/M3):
#   brew install cmake dlib imagemagick
#
# The application will automatically use the correct library based on your OS.
# ============================================================================

